<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wms">
<select id="userLogin" resultType="int" parameterType="map">
select count(*) from wms.wms_user_info where user_id = #{user_id} and user_pw = #{user_pw};
</select>

</mapper>
  